{
    "build": {
        "builder": "DOCKERFILE"
    },
    "deploy": {
        "startCommand": "gunicorn --bind 0.0.0.0:$PORT app:app --timeout 300 --workers 1",
        "healthcheckPath": "/health",
        "healthcheckTimeout": 30
    }
}