{
    "build": {
        "builder": "DOCKERFILE"
    },
    "deploy": {
        "startCommand": "python -m gunicorn --bind 0.0.0.0:$PORT app:app --timeout 300 --workers 1 --log-level info",
        "healthcheckPath": "/health",
        "healthcheckTimeout": 30
    }
}